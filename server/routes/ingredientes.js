// import { Router } from 'express';
// const router = Router();


// router.get('/ingredientes', (req, res) => {
//   const sql = 'SELECT * FROM _listaingredientes';
//   db.query(sql, (err, results) => {
//     if (err) throw err;  // Considera manejar este error de una manera más amigable para el usuario.
//     res.json(results);
//   });
// });

// export default router;